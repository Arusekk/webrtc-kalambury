extends base.pug

block variables
  - var title = 'Rooms'

block content
  main#lobbyMain
    a.newRoom(href=`/draw#${Math.floor(Math.random() * 1e6)}`) Nowy pokój
    ul.roomList
      each key of room.keys()
        li
          a(href=`/view#${encodeURIComponent(key)}`) Pokój #{key}
