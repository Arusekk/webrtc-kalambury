doctype html
html
  head
    title Rooms — kalambury
  body
    h1 Kalambury
    p
      a(href='/draw.html#${Math.floor(Math.random() * 1e6)}') Nowy pokój
    ul
      each key in Object.keys(roomOwner)
        li
          a(href=`/viewer.html#${encodeURIComponent(key)}`) Pokój #{key}
